name: python-stdeb
version: 0.2.3
description: generate debian package using stdeb
properties:
  project_root:
    type: string
    default: $WERCKER_ROOT
    required: false
  clear_debian_source_dir:
    type: string
    default: "true"
    required: false
  clean_debian_rules:
    type: string
    default: "true"
    required: false
  stdeb_command:
    type: string
    default: debianize
    required: false
